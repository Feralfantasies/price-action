# price-action Knowledge Bundle — Update Log

## 2026-09-17
* **Create**: [Paper Trading Accounting](paper-trading-accounting.md) — the authoritative reference for how `src/accounting.rs` prices replay signals as a funded paper account (entry/exit at bar closes, flat bps fees per side, short collateral, insufficient-funds skip, UTC-day math, session totals), with "what this model is not" boundaries. New concept (`status: draft`).
* **Update**: Replay report gained funded paper-accounting output — new config knobs `starting_balance` / `PRICE_ACTION_STARTING_BALANCE` (f64, default 10 000, must be finite and > 0) and `trade_fee_bps` / `PRICE_ACTION_TRADE_FEE_BPS` (int ≥ 0, default 5 bps = 0.05% per side); trace lines now carry `cash=`/`equity=` after every bar with `(insufficient funds)` notes when an entry is skipped; new report sections — closed trade table (net of fees), totals per UTC day (24h), session totals — above the existing footer line ([Replay Workflow](replay-workflow.md)). New module `src/accounting.rs` added to the module maps in [Overview](overview.md) and `README.md`; its tests pin independently computed reference values.
* **Update**: [Configuration](configuration.md) settings table gained the two paper-account rows (and their validation/constraints); the uniqueness claim on the threshold row was removed. [Sample Bar File](sample-bars.md) "Known output" now includes the expected trade/roll-up lines for the unmodified sample at defaults. `README.md`'s expected-output block, reading notes, layout and configuration sections were refreshed to match the new report shape. Example TOML gained commented entries for both knobs.
* **Update**: All concepts touched by this change keep/become `status: draft` pending a named human review; [Replay Workflow](replay-workflow.md) links the model details to the new reference page instead of repeating them.

## 2026-09-16
* **Update**: Addressed automated (CodeRabbit) review findings on PR #7: corrected the clippy invocation to `cargo clippy --all-targets -- -D warnings` ([AGENTS.md](../AGENTS.md), [Container Image & Release](container-image-and-release.md), [Development Workflow](development-workflow.md)); narrowed the engine *No drift* invariant to what the code actually guarantees — retries block **additional** bar consumption; they do not prevent the temporary strategy-ahead state after a failed fresh execution ([Trading Engine](engine.md)); reworded replay's guarantee to "same shared application engine + strategy used by every run" (no live-mode claim) in [Replay Workflow](replay-workflow.md) and `README.md`; made row-level vs file-level error numbering explicit in the [Bar File Format](bar-file-format.md) table; added mandatory `docs/log.md` update steps to the [Configuration](configuration.md) and [Sample Bar File](sample-bars.md) maintenance checklists. All concepts stay `status: draft` until a named human review.
* **Creation**: Established the [OKF v0.2](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md) knowledge bundle in `docs/`: [Overview](overview.md), [Replay Workflow](replay-workflow.md), [Trading Engine](engine.md), [Consecutive Closes Strategy](strategy-consecutive-closes.md), [Market Data Model](market-data-model.md), [Execution Layer](execution-layer.md), [Bar File Format](bar-file-format.md), [Sample Bar File](sample-bars.md), [Configuration](configuration.md), [Container Image & Release](container-image-and-release.md), [Development Workflow](development-workflow.md), [Disclaimer & Risk](disclaimer-risk.md), and this bundle's own [Update Guide](bundle-update-guide.md).
* **Rule change**: The repository now carries an `AGENTS.md` that mandates reading this bundle before any change and keeping it current with every behaviour-affecting PR (see the workspace agent rules for the disclosure workflow).
